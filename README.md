# GraphQL with Node.js Server

This is my opportunity to learn how GraphQL and Node work together.

starting with `{ makeExecutableSchema } from 'graphql-tools';`

#### type Link
* id
* url
* description

* Query
  * allLinks()
